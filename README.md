In the organization, this agent application is used to send information about active and currently opened applications from user systems to the server through a Flask API.

The application is configured to start automatically during system startup so that monitoring begins when the system is turned on.

The agent runs silently in the background and remains hidden from users, ensuring uninterrupted data collection without affecting user workflow.
